#pragma once

/** @brief Provides the declaration for the `FileRef` class. */

#include <IcelinSdk.PvZGW2/Core/String.h>

namespace fb
{

class FileRef : public String {
  /* This is essentially a wrapper around the `String` type; it
     is pretty much reserved for future use.
  */
};

}
